import 'package:flutter/material.dart';

class CostTabScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Cost"),),);
  }
}